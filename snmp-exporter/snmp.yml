# SNMP Exporter Configuration for First Light
# Format: v0.23.0+ (auth split from modules)

auths:
  public_v2:
    community: public
    version: 2

modules:
  # Standard interface statistics (works with most network devices)
  if_mib:
    walk:
      - sysUpTime
      - ifTable
      - ifXTable
